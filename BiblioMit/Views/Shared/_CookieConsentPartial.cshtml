@using Microsoft.AspNetCore.Http.Features

@{
    ITrackingConsentFeature? consentFeature = Context.Features.Get<ITrackingConsentFeature>();
    bool showBanner = !consentFeature?.CanTrack ?? false;
    string? cookieString = consentFeature?.CreateConsentCookie();
}

@section Header{
    @*@if (showBanner)*@
    @if (true)
    {
        <div class="modal-dialog modal-frame modal-bottom">
            <div class="modal-content rounded-0">
                <div class="modal-body py-1">
                    <div class="d-flex justify-content-center align-items-center my-3">
                        <p class="mb-0">@Localizer["We use cookies to improve your website experience"]</p>
                        <button type="button" class="btn btn-success btn-sm ms-2" data-cookie-string="@cookieString"
                                data-mdb-dismiss="modal">
                            @Localizer["Ok, thanks"]
                        </button>
                        <button type="button" class="btn btn-primary btn-sm ms-2">@Localizer["Learn more"]</button>
                    </div>
                </div>
            </div>
        </div>
        @*<div id="cookieConsent" class="alert alert-info alert-dismissible fade show fixed-bottom" role="alert">
                @Localizer["BiblioMit uses cookies and personal information to function correctly. By continuing to use BiblioMit you are giving consent to cookies being used."]
                <a asp-area="" asp-controller="Home" asp-action="Privacy">@Localizer["Learn More"]</a>.
                <button type="button" class="btn btn-sm btn-success accept-policy close" data-dismiss="alert"
                        aria-label="Close" data-cookie-string="@cookieString" data-mdb-toggle="animation"
                        data-mdb-animation-reset="false" data-mdb-animation="fade-out-down">
                    <span aria-hidden="true">@Localizer["Accept"]</span>
                </button>
            </div>*@
    }
}
