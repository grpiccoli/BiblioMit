@model BiblioMit.Models.BootstrapModel

<partial name="_Libs" model='new List<string> { "modal" }' />

<div aria-hidden="true" aria-labelledby="@Model.AreaLabeledId" role="dialog" tabindex="-1" id="@Model.Id" class="modal fade">
    <div class="modal-dialog @Model.ModalSizeClass" role="document">
        <div class="modal-content">          
        </div>
    </div>
</div>